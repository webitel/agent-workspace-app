# Copy to `.env.e2e.local` (gitignored) and fill in a long-lived access token
# for the instance configured in `.env.e2e`.
#
# Without this token the `live` Playwright project is skipped; the `mocked`
# project always runs.
E2E_ACCESS_TOKEN = ''
